import { defineMessages } from 'react-intl';

export const scope = 'boilerplate.containers.Notes';

export default defineMessages({
  header: {
    id: `${scope}.notes.header`,
    defaultMessage: 'Your notes',
  },
});
